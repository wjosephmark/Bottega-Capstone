import React, { Component } from "react"

export default class Checkout extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return(
            <h1>Checkout</h1>
        )
    }
}